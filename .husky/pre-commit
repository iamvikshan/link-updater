# 1. Enforce global identity (instant execution)
./.husky/identity-guard.sh

# Run ESLint & Prettier on staged files only to avoid memory limit crashes (SIGTERM/code 143)
bunx lint-staged || exit 1

# Run type-checking on the whole project
bunx tsgo --noEmit || exit 1

echo "Pre-commit checks passed successfully."